If we want to install RUST compiler, we need to download *rustup* : its an command line tool for managing rust versions and tools which is associated to rust

If Rust is already installed , just use this command to update

```
rustup update
```

